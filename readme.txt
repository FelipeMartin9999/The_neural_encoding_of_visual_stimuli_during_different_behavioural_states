The function of each code is as follows:

load Data: Processes the original data and transforms it into .parquet files.

data analysis: Takes the parquet files as input and generates the graphs needed for the analysis.

machine learning models: Takes the parquet files as input and generates the machine learning models needed for the analysis.